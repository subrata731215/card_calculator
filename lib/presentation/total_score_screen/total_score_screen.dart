import 'package:flutter/material.dart';

class TotalScoreScreen extends StatelessWidget {
  const TotalScoreScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('total Score Screen Date Wise'),
      ),
    );
  }
}
